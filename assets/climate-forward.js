(()=>{
const isProfile=Boolean(document.body.dataset.country),base=isProfile?'../':'';
const css=document.createElement('link');css.rel='stylesheet';css.href=base+'assets/climate-forward.css';document.head.appendChild(css);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const D=window.PUBLIC_COUNTRY_DATA||{countries:{}};
const names={KEN:'Kenya',UGA:'Uganda',RWA:'Rwanda',BDI:'Burundi',ETH:'Ethiopia',ZMB:'Zambia'};
const fmtPct=n=>n==null?'No current CHIRPS value':`${Number(n)>=0?'+':''}${Number(n).toFixed(0)}% vs long-term average`;
function sourceLinks(c,code){const links=[];if(c?.weekly?.url)links.push(`<a href="${esc(c.weekly.url)}" target="_blank" rel="noreferrer">Weekly forecast ↗</a>`);if(c?.seasonal?.url)links.push(`<a href="${esc(c.seasonal.url)}" target="_blank" rel="noreferrer">Seasonal outlook ↗</a>`);if(c?.drought?.url)links.push(`<a href="${esc(c.drought.url)}" target="_blank" rel="noreferrer">Drought Watch ↗</a>`);if(code!=='ZMB')links.push('<a href="https://eahazardswatch.icpac.net/mapviewer/" target="_blank" rel="noreferrer">Hazards Watch map ↗</a>');return [...new Set(links)].join(' · ')}
function scopeBadge(scope){if(scope==='regional')return '<span class="climate-scope regional">Regional signal</span>';if(scope==='not-covered')return '<span class="climate-scope muted-scope">Not covered</span>';return ''}
function label(text,scope){return `<div class="climate-forward-label">${esc(text)}${scopeBadge(scope)}</div>`}
function nowPanel(code,c){const rain=D.countries?.[code]?.rainfall?.anomalyPct;return `<div class="climate-forward-panel"><h3>Now</h3>${label('Observed rainfall')}<p><strong>${esc(fmtPct(rain))}</strong></p><div class="climate-forward-note">Latest one-month CHIRPS period.</div>${label('Drought watch',c?.drought?.scope)}<div class="climate-forward-drought">${esc(c?.drought?.text||'No current drought-watch summary available.')}</div></div>`}
function weekPanel(c){const w=c?.weekly||{};return `<div class="climate-forward-panel"><h3>Current 7-day outlook</h3><div class="climate-forward-period">${esc(w.period||'Latest weekly outlook')}</div>${label('Rainfall',w.rainfallScope)}<p>${esc(w.rainfall||'No current weekly rainfall summary.')}</p>${label('Temperature',w.temperatureScope)}<p>${esc(w.temperature||'No current weekly temperature summary.')}</p>${label('Exceptional rainfall',w.heavyRainScope)}<p>${esc(w.heavyRain||'No current exceptional-rainfall summary.')}</p>${label('Heat stress',w.heatStressScope)}<p>${esc(w.heatStress||'No current heat-stress summary.')}</p>${label('Flood watch',w.floodScope)}<p>${esc(w.flood||'No current flood summary.')}</p></div>`}
function seasonPanel(c){const s=c?.seasonal||{};return `<div class="climate-forward-panel"><h3>Season ahead</h3><div class="climate-forward-period">${esc(s.period||'Latest seasonal outlook')}</div>${label('Rainfall',s.rainfallScope)}<p>${esc(s.rainfall||'No current seasonal rainfall summary.')}</p>${label('Temperature',s.temperatureScope)}<p>${esc(s.temperature||'No current seasonal temperature summary.')}</p></div>`}
function fullBlock(code,c){return `<div class="climate-forward-wrap"><div class="climate-forward-grid">${nowPanel(code,c)}${weekPanel(c)}${seasonPanel(c)}</div><div class="climate-forward-sources">${sourceLinks(c,code)}</div><div class="climate-forward-disclaimer">Country/sub-country text is extracted from the areas ICPAC explicitly names in its bulletin. A regional signal is labelled separately and should not be read as a country-specific forecast. “No specific signal highlighted” is not the same as “no risk”.</div></div>`}
function compactBlock(code,c){return `<div class="climate-forward-compact"><div class="climate-forward-grid">${nowPanel(code,c)}${weekPanel(c)}${seasonPanel(c)}</div><div class="climate-forward-sources">${sourceLinks(c,code)}</div></div>`}
function addSourceRows(F){const body=document.getElementById('sourcesBody');if(!body)return;const labels={'ICPAC weekly forecast':'Current 7-day rainfall, temperature, heavy-rainfall, heat-stress and flood context','ICPAC seasonal forecast':'Next-season rainfall and temperature outlook','East Africa Drought Watch':'Combined Drought Indicator regional context and local drill-down','SADC / SARCOF-33':'Zambia seasonal rainfall and temperature outlook'};for(const[key,s]of Object.entries(F.sources||{})){if(body.querySelector(`[data-forward-source="${CSS.escape(key)}"]`))continue;const tr=document.createElement('tr');tr.dataset.forwardSource=key;tr.innerHTML=`<td>${esc(key)}</td><td>${esc(s?.status||'available')}</td><td>${esc(labels[key]||'Forward-looking climate context')}</td>`;body.appendChild(tr)}}
async function run(){try{const F=await fetch(base+'data/climate-forward.json?v='+Date.now(),{cache:'no-store'}).then(r=>{if(!r.ok)throw Error('forecast data unavailable');return r.json()});
  if(isProfile){const code=document.body.dataset.country,c=F.countries?.[code];if(c){const target=document.getElementById('climateSummary');if(target){const status=target.querySelector('.value')?.outerHTML||'';const h=target.closest('.card')?.querySelector('h2');if(h)h.textContent='Current & upcoming climate';target.innerHTML=`${status}${fullBlock(code,c)}`}}}
  else{for(const code of Object.keys(names)){const c=F.countries?.[code],card=document.getElementById(`climate-${code}`),detail=card?.querySelector('.detail');if(!c||!detail)continue;const summary=[...card.children].find(x=>x.tagName==='P');if(summary&&c.seasonal?.rainfall)summary.textContent=c.seasonal.rainfall;[...detail.querySelectorAll(':scope > p')].forEach(p=>{const t=p.textContent.trim();if(/^Temperature:|^Observed rainfall:|^Seasonal source:/i.test(t))p.remove()});detail.querySelector('.climate-forward-compact')?.remove();detail.insertAdjacentHTML('afterbegin',compactBlock(code,c))}addSourceRows(F)}
}catch(e){console.warn('Current/upcoming climate layer unavailable:',e.message)}}
run();
})();
